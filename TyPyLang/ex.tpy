use strict

interface counter:
   counter:int = 0

class ClassName() implements counter:
    def __init__(self, counter:int) -> None:
       self.counter:int = counter
    def start(self) -> None:
        while True:
            option:str = input()
            if option == "1":
                self.counter +=1
            else:
                print("Counter:", self.counter)
                break

game:auto = ClassName(0)
game.start()

int x = 0